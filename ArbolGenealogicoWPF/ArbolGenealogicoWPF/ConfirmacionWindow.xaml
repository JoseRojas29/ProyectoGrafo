<local:WindowBaseLogica x:Class="ArbolGenealogicoWPF.ConfirmacionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ArbolGenealogicoWPF"
        SizeToContent="Manual"
        Height="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FontFamily="pack://application:,,,/Fonts/#Orbitron">

    <Window.Resources>
        <!-- TextBlock -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FF6F00"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- Button -->
        <Style TargetType="Button">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="#1A0033"/>
            <Setter Property="Foreground" Value="#FFAB40"/>
            <Setter Property="BorderBrush" Value="#FF9100"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="8,0,8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="2"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#180028"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#B833FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2A003F"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#B833FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#000000">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barra superior -->
        <Border Grid.Row="0" Background="#1A0033"
                MouseLeftButtonDown="Window_MouseLeftButtonDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Confirmación"
                           VerticalAlignment="Center"
                           Foreground="#FF6F00"
                           FontSize="16"
                           FontWeight="Bold"
                           Margin="12,0,0,0"/>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Right"
                            Margin="0,0,8,0">
                    <Button Content="✖" Width="40" Height="28" Padding="0" FontSize="14"
                            VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                            Click="Cancelar_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Contenido principal -->
        <StackPanel Grid.Row="1" Margin="16" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Crear un nuevo árbol implica eliminar por completo el árbol actual&#x0a;¿Desea continuar el procedimiento?"
                       FontWeight="Bold" FontSize="30" TextAlignment="Center" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Aceptar" Width="150" Click="Aceptar_Click"/>
                <Button Content="Cancelar" Width="150" Click="Cancelar_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</local:WindowBaseLogica>